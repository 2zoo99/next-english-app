// agenda.txt
// 계획 파일
// 내림차순 (날짜)
----------------------------------------------------
2026-04-16
    - to do # 1
        - sentence list page
            - add 'make quiz' component 
                - drawer function
                - choose sentence from sentence list
                - click eng word and drag toward a trans word
                - store the pair of two words to quiz table
                - add hint block and button
                - every hint can be the eng meaning of the words 
    - to do # 2
        - sentence quiz page    
            - based on the pair of word and trans blank quiz start
            - be able to switch mode which could be normal or random
            - words should have 품사 feature on table in db
            - 품사 will be displayed when user want to see a hint
            - the quiz has choosing mode, writing mode 
----------------------------------------------------

generator client {
  provider = "prisma-client"
  
}

datasource db {
  provider  = "postgresql"
  //url       = env("DATABASE_URL")
  //directUrl = env("DIRECT_URL")
}

----------------------------------------------------
2026-04-14
    - working-branch 
        - test/component
    
    - trouble-shooting  
        V- completed 영어 문장 연습 컴포넌트에서 숫자를 입력하면 단어로 인식못하는지 계속 틀렸다고 함. 

    - todos
        - CSS 로 컴포넌트 사용성 더하기
        - 문장 목록 리스트 수정날짜 내림차순
        - 네비게이션 바 생성 - 문장,해석 추가 페이지, 추가한 문장 목록 페이지, 연습 페이지
            - 네비게이션 바 만들기
                - 파일구조
                    components/
                    layout/
                        Sidebar.tsx
                        TopBar.tsx
                        Layout.tsx
                    app/
                    layout.tsx        ← 여기서 Layout 감싸기
                    page.tsx
                - 파일 구조 변경
                    app/
                        (with-nav)/          ← 폴더명에 ()를 쓰면 URL에 영향 없음
                            layout.tsx         ← 여기에 Layout(네비게이션) 적용
                            dashboard/
                                page.tsx         ← /dashboard 로 접근 가능
                            users/
                                page.tsx         ← 나중에 네비게이션 필요한 페이지는 여기에
                        (no-nav)/
                            layout.tsx         ← 기본 레이아웃만 (네비게이션 없음)
                            page.tsx           ← / (홈, 랜딩 등)
                            login/
                                page.tsx
                        layout.tsx           ← 루트: <html>, <body> 만 담당

        - 문제 만들기 기능 만들기
            - 문장과 해석 문장 각각 작성해서 추가한뒤.
            - 문장 목록 페이지에서 문제 만들기 클릭
            - 원하는 문장 클릭
            - 새로운 팝업창에서 문제 만들기
            
            - 문제 만들기 버튼 클릭
            → isDrawerOpen = true → 드로어 등장
            → 목록에서 문장 클릭
            → selectedSentence에 저장, isDrawerOpen = false
            → 선택된 문장으로 문제 시작

----------------------------------------------------
2023-04-07 
    - working branch
        - test/component

    - completed
        - PracticeForm 업데이트
        - agenda.txt 생성 

    - trouble-shooting
        V-completed 문장 입력 후 목록은 업데이트되는데, 연습 모듈의 문제가 업데이트되지 않음. 
        V-completed 페이지 height 불편함 문제
        V- 문장 입력 -> 저장 후 수정 기능 추가 안했음. (해야됨)
        - 브랜치 main에 merge 해야함.

    - error-handling
        - ./../ ~ 처럼 상대경로로 fetch를 하면 오류 발생함. 
            컴포넌트가 어떤 page.tsx에 있느냐에 따라 상대경로가 달라지기 때문. 
            -> 절대경로로 바꿔서 오류를 없애주면 됨. 

